import AboutSection from '@/components/hospital/AboutSection';

export const metadata = {
  title: 'About | MedVista Premier',
  description: 'Learn about MedVista Premier — our story, values, leadership, and the legacy of healing that has guided us since 1998.',
};

export default function AboutPage() {
  return <AboutSection />;
}
