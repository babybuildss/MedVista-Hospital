import ContactSection from '@/components/hospital/ContactSection';

export const metadata = {
  title: 'Contact | MedVista Premier',
  description: 'Book an appointment or reach out to MedVista Premier. Emergency line, outpatient timings, and all contact information.',
};

export default function ContactPage() {
  return <ContactSection />;
}
